[package]
name = "contime"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
repository = "https://github.com/yourname/contime"

[dependencies]
tokio = { version = "1.37", features = ["rt", "time", "sync"] }
