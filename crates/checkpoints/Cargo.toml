[package]
name = "contime-checkpoints"
version = "0.1.0"
edition = "2021"
autobenches = false
license = "MIT"
description = "Isolated apply-time checkpoint replay for ConTime"
publish = false

[dependencies]

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
