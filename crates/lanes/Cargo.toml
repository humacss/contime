[package]
name = "contime-lanes"
version = "0.1.0"
edition = "2021"
autobenches = false
license = "MIT"
description = "Isolated compile-time lane projection and application contracts for ConTime"
publish = false

[dependencies]

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
